# OAuth Service

Сервис для авторизации через OAuth 2.0 с поддержкой множественных провайдеров.

## Описание

Приложение на FastAPI, которое предоставляет:
- 🔐 Авторизацию пользователей через **Google OAuth 2.0**
- 🔴 Авторизацию пользователей через **Yandex OAuth 2.0**
- 📅 Получение информации о ближайшем событии из **Google Calendar**
- 👤 Получение данных профиля пользователя **Яндекс**
- 🎨 Современный веб-интерфейс с фирменными кнопками провайдеров
- 🔒 **Authorization Code Flow** для безопасной авторизации
- ✨ Интерактивные карточки с данными и возможностью закрытия

## Технологии

- **Python 3.11+**
- **FastAPI** - веб-фреймворк
- **Uvicorn** - ASGI сервер
- **aiohttp** - асинхронный HTTP клиент для Google API
- **Pydantic** - валидация данных и настроек
- **uv** - менеджер зависимостей
- **Docker** - контейнеризация

## Установка и запуск

### Локальный запуск

1. Установите зависимости:
```bash
pip install uv
uv sync
```

2. Настройте переменные окружения (см. раздел "Настройка")

3. Запустите приложение:
```bash
python src/main.py
```

4. Откройте в браузере: http://127.0.0.1:8000

### Запуск через Docker

1. Настройте переменные окружения в файле `.env`

2. Запустите контейнер:
```bash
docker-compose -f docker-compose.local.yml up
```

3. Откройте в браузере: http://localhost:8000

## Настройка

Скопируйте `.env.template` в `.env` и заполните следующие параметры:

### Google OAuth

Для работы с Google необходимо создать OAuth 2.0 credentials в [Google Cloud Console](https://console.cloud.google.com/):

1. Создайте проект или выберите существующий
2. Включите **Google Calendar API**
3. Создайте **OAuth 2.0 Client ID** (тип: Web application)
4. Добавьте authorized redirect URI: `http://127.0.0.1:8000/api/google/auth/callback`
5. Настройте **OAuth consent screen** и добавьте scopes:
   - `openid`
   - `https://www.googleapis.com/auth/userinfo.email`
   - `https://www.googleapis.com/auth/calendar.readonly`
6. Скопируйте Client ID и Client Secret

```bash
# Google OAuth credentials
GOOGLE__OAUTH__CLIENT_ID=your_google_client_id
GOOGLE__OAUTH__CLIENT_SECRET=your_google_client_secret
GOOGLE__OAUTH__REDIRECT_URI=http://127.0.0.1:8000/api/google/auth/callback
```

### Yandex OAuth

Для работы с Яндекс необходимо зарегистрировать приложение на [Яндекс.OAuth](https://oauth.yandex.ru/):

1. Перейдите на [oauth.yandex.ru](https://oauth.yandex.ru/) и войдите в аккаунт
2. Нажмите **"Зарегистрировать новое приложение"**
3. Заполните информацию о приложении
4. В **Callback URI** добавьте: `http://127.0.0.1:8000/api/yandex/auth/callback`
5. Базовые разрешения включены по умолчанию (доступ к логину, email)
6. Скопируйте **ID** и **Пароль** приложения

```bash
# Yandex OAuth credentials
YANDEX__OAUTH__CLIENT_ID=your_yandex_client_id
YANDEX__OAUTH__CLIENT_SECRET=your_yandex_client_secret
YANDEX__OAUTH__REDIRECT_URI=http://127.0.0.1:8000/api/yandex/auth/callback
```

**⚠️ Важно:** При локальном запуске открывайте сайт через `http://127.0.0.1:8000`, а не через `localhost:8000`, так как это разные домены для cookies!

### Настройки сервера

```bash
# Автоперезагрузка при изменении кода (для разработки)
SERVER__RELOAD=True
```

### Настройки безопасности

```bash
# CORS: разрешенные origins
SECURITY__ALLOWED_ORIGINS=["http://localhost:8000","http://127.0.0.1:8000"]

# Cookie settings (в production используйте Secure=True с HTTPS)
SECURITY__COOKIE_SECURE=False
SECURITY__COOKIE_SAMESITE=strict
```

## API Endpoints

### Общие

- `GET /` - главная страница с интерфейсом

### Google OAuth

- `GET /api/google/auth/login` - инициирует OAuth flow с Google
- `GET /api/google/auth/callback` - callback endpoint для обработки ответа от Google
- `GET /api/google/calendar/next-event` - получить ближайшее событие из Google Calendar (требует авторизации)

### Yandex OAuth

- `GET /api/yandex/auth/login` - инициирует OAuth flow с Яндекс
- `GET /api/yandex/auth/callback` - callback endpoint для обработки ответа от Яндекс
- `GET /api/yandex/user/info` - получить информацию о пользователе Яндекс (требует авторизации)

## Использование

### Google Calendar

1. Откройте главную страницу: `http://127.0.0.1:8000`
2. Нажмите кнопку **"Войти через Google"** (белая с цветным логотипом)
3. Авторизуйтесь и предоставьте доступ к Google Calendar
4. После успешной авторизации нажмите **"📅 Получить событие из Google Календаря"**
5. Приложение отобразит информацию о ближайшем предстоящем событии

### Yandex Profile

1. Откройте главную страницу: `http://127.0.0.1:8000`
2. В разделе **"🔐 Авторизация"** нажмите кнопку **"Войти через Яндекс"** (красная с иконкой "Я")
3. Авторизуйтесь через Яндекс ID
4. В разделе **"📊 Получить данные"** нажмите **"👤 Получить данные профиля Яндекс"**
5. Увидите карточку с данными:
   - ID пользователя
   - Логин
   - Email
   - Отображаемое имя

## Структура проекта

```
oauth-service/
├── src/
│   ├── main.py                      # Точка входа приложения, lifespan management
│   ├── api/
│   │   ├── router.py                # Главный API роутер
│   │   ├── routes/
│   │   │   ├── google.py            # Google OAuth & Calendar endpoints
│   │   │   └── yandex.py            # Yandex OAuth & User Info endpoints
│   │   └── deps/
│   │       ├── auth.py              # OAuth initialization, cookie schemes
│   │       ├── cookies.py           # Helper функции для работы с cookies
│   │       ├── validators.py        # OAuth state validators (CSRF защита)
│   │       └── getters.py           # Dependency getters для клиентов
│   ├── core/
│   │   ├── settings.py              # Конфигурация через Pydantic Settings
│   │   ├── constants.py             # Константы (имена cookies, timeouts)
│   │   └── templates.py             # Jinja2 templates configuration
│   ├── integrations/
│   │   ├── base_api_client.py       # Базовый API клиент (Singleton)
│   │   ├── google/
│   │   │   ├── client.py            # Google API клиент (Calendar, UserInfo)
│   │   │   ├── schemas.py           # Pydantic модели для Google API
│   │   │   └── exceptions.py        # Google API exceptions
│   │   └── yandex/
│   │       ├── client.py            # Yandex API клиент (UserInfo)
│   │       ├── schemas.py           # Pydantic модели для Yandex API
│   │       └── exceptions.py        # Yandex API exceptions
│   └── static/
│       └── html/
│           └── index.html           # Интерактивный веб-интерфейс
├── .env                              # Переменные окружения (не в git)
├── .env.template                     # Шаблон конфигурации
├── pyproject.toml                    # Зависимости проекта (uv)
├── Dockerfile                        # Docker образ
└── docker-compose.local.yml          # Docker Compose конфигурация
```

## Архитектура и особенности

### OAuth Flow

Приложение использует **Authorization Code Flow** для обоих провайдеров:
1. **Login** - редирект на страницу авторизации провайдера с CSRF state token
2. **Callback** - обмен authorization code на access token на стороне сервера
3. **Token Storage** - безопасное хранение токенов в HTTPOnly cookies

### Безопасность

Приложение использует следующие механизмы безопасности:
- ✅ **CSRF защита** через OAuth state parameter с использованием `secrets.compare_digest()`
- ✅ **HTTPOnly cookies** для защиты токенов от XSS атак
- ✅ **Secure cookies** (в production с HTTPS)
- ✅ **SameSite=lax** для OAuth flow, **SameSite=strict** для access tokens
- ✅ **CORS middleware** с whitelist origins
- ✅ **Security headers**: X-Content-Type-Options, X-Frame-Options, X-XSS-Protection, Referrer-Policy
- ✅ **Валидация входных данных** через Pydantic
- ✅ **Sanitized error messages** - не раскрываем внутренние детали API

### Архитектурные решения

#### DRY принцип
- Общие helper функции для работы с cookies (`cookies.py`)
- Единый валидатор OAuth state для обоих провайдеров
- Базовый `OAuthInitData` dataclass с type aliases
- Базовый `BaseAPIClient` с Singleton pattern

#### Dependency Injection
- FastAPI DI для передачи клиентов и валидаторов
- Чистые, легко тестируемые функции
- Разделение concerns: auth, validation, cookies

#### Singleton Pattern
- API клиенты (GoogleClient, YandexClient) используют Singleton
- Единая aiohttp session на весь lifecycle приложения
- Правильное управление ресурсами через lifespan

#### Type Safety
- Полная типизация через Python type hints
- Pydantic модели для всех API схем
- TypeAlias для читаемости кода

## Разработка

### Код стайл
- Асинхронный код (async/await) для всех I/O операций
- Pydantic Settings для конфигурации с валидацией
- Константы вынесены в `core/constants.py`
- Docstrings для всех публичных функций

### Интерфейс

Веб-интерфейс разделен на две секции:

**🔐 Авторизация**
- Кнопки входа через Google и Яндекс
- Фирменные стили провайдеров

**📊 Получить данные**
- Кнопки для получения информации после авторизации
- Интерактивные карточки с данными
- Возможность закрыть карточку крестиком (×)
- Плавная анимация при закрытии

### Добавление нового OAuth провайдера

1. Создайте директорию `src/integrations/<provider>/`
2. Добавьте `client.py`, `schemas.py`, `exceptions.py`
3. Наследуйте клиент от `BaseAPIClient`
4. Добавьте конфигурацию в `core/settings.py`
5. Создайте отдельную cookie для провайдера в `core/constants.py`
6. Создайте роутер в `src/api/routes/<provider>.py`
7. Используйте существующие helpers из `api/deps/`

## Troubleshooting

### Cookie не сохраняется

**Проблема:** После авторизации cookie отсутствует, ошибка "State cookie missing"

**Решение:** Убедитесь, что:
- Открываете сайт через `http://127.0.0.1:8000`, а не `localhost:8000`
- Redirect URI в настройках провайдера точно совпадает с `REDIRECT_URI` в `.env`
- Используете один и тот же домен (127.0.0.1 или localhost) везде

### Docker networking issues

Если приложение запущено в Docker, убедитесь, что:
- Redirect URI использует адрес, доступный с вашего хоста
- Cookie domain настроен правильно
